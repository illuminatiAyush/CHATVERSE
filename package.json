{
  "name": "chatverse",
  "private": true,
  "scripts": {
    "dev": "npx --yes concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\""
  },
  "workspaces": [
    "client",
    "server"
  ]
}
